query PokemonListV2($limit: Int!, $offset: Int!, $search: String) {
    pokemon_v2_pokemon(
        limit: $limit,
        offset: $offset,
        order_by: {id: asc},
        where: {name: {_ilike: $search}}
    ) {
        id
        name
        pokemon_v2_pokemontypes {
            pokemon_v2_type {
                name
            }
        }
    }
}
